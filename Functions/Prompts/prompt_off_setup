# Converted to zsh prompt theme by bash2zshprompt, written by <adam@spiers.net>
# Very simple prompt
prompt_off_setup () {
  PS1="%# "
  PS2="> "

  prompt_opts=( cr percent )
  precmd () { }
  preexec () { }
}

prompt_off_setup "$@"
