name=zsh/termcap

link='if test "x$ac_cv_func_tgetent" = xyes; then
          if test "x$zsh_cv_shared_tgetent" = xyes; then
	      echo either
	  else
	      echo static
	  fi
      else
          echo no;
      fi
'
load=yes

autobins="echotc"

objects="termcap.o"
