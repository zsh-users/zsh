#compdef builtin

if (( $CURRENT > 2 )); then
  shift words
  (( CURRENT -- ))
  _normal
else
  local expl

  _tags any:command commands || return 1

  _description expl 'builtin command'
  compadd "$expl[@]" "$@" - "${(k@)builtins}"
fi
