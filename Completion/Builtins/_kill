#defcomp kill

local list

if [[ -iprefix '-' ]]; then
  compgen -k "($signals[1,-3])"
else
  compgen -P '%' -j
  list=("$(ps 2>/dev/null)")
  compgen -y '$list' -s '`ps 2>/dev/null | tail +2 | cut -c1-5`'
fi
