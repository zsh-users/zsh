#defcomp -condition-

local prev="$words[CURRENT-1]"

if [[ "$prev" = -o ]]; then
  compgen -o -M 'L:|[nN][oO]= M:_= M:{A-Z}={a-z}'
elif [[ "$prev" = -([no]t|ef) ]]; then
  _files
else
  local ret=1

  _files && ret=0
  compgen -v && ret=0

  return ret
fi
